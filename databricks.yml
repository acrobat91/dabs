# This is a Databricks asset bundle definition for test_ci_cd_local.
# The Databricks extension requires databricks.yml configuration file.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.

bundle:
  name: test_ci_cd_local

include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-4235127113959330.10.azuredatabricks.net

  ## Optionally, there could be 'staging' or 'prod' targets here.
  prod:
    workspace:
      host: https://adb-4235127113959330.10.azuredatabricks.net
